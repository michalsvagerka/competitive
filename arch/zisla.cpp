#include "../l/lib.h"
// #include "../l/mod.h"

class zisla {
public:
    void solve(istream& cin, ostream& cout) {
        int T; cin >> T;
        for (int t = 0; t < T; ++t) {
            ll k; cin >> k;
            cout << k*3 << '\n';
        }
    }
};
