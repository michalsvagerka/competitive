#include "../l/lib.h"
// #include "../l/mod.h"

struct z { int a,b,c; };

class balenie {
public:
	void solve(istream& cin, ostream& cout) {
		int j, y; cin >> j >> y;
		z c = {0,0,0};

		for (int i = 0; i < j; ++i) {
			string s; cin >> s;
			int ind = 0; bool ch = false;
			for (char d: s) {
				if (ch && d != '.')
			}
		}
	}
};
